---
import AppointmentCTA from '../components/AppointmentCTA.astro';
import FAQSection from '../components/FAQSection.astro';
import Hero from '../components/Hero.astro';
import ServiceCards from '../components/ServiceCards.astro';
import DentistLayout from '../layouts/DentistLayout.astro';
import type { AppointmentCtaData, FAQSectionData, FooterData, HeroData, NavigationData, SeoPageData, ServiceSectionData, SiteData } from '../types';

interface Props {
  appointmentCta: AppointmentCtaData;
  baseLayout: unknown;
  faq: FAQSectionData;
  footer: FooterData;
  hero: HeroData;
  navigation: NavigationData;
  seo: SeoPageData;
  services: ServiceSectionData;
  site: SiteData;
}

const { appointmentCta, baseLayout, faq, footer, hero, navigation, seo, services, site } = Astro.props;
---

<DentistLayout baseLayout={baseLayout} title={seo.title} description={seo.description} footer={footer} lang={seo.lang} navigation={navigation} site={site}>
  <Hero hero={hero} />
  <ServiceCards section={services} />
  <FAQSection section={faq} />
  <AppointmentCTA cta={appointmentCta} />
</DentistLayout>
